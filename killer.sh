#!/bin/bash
tmux kill-session -t sess1 \;
tmux kill-session -t sess2 \;
tmux kill-session -t sess3 \;
tmux kill-session -t sess4 \;
tmux kill-session -t sess5 \;
tmux kill-session -t sess6 \;
tmux kill-session -t sess7 \;
tmux kill-session -t sess8 \;
tmux kill-session -t sess9 \;
tmux kill-session -t sess10 \;
tmux kill-session -t sess11 \;